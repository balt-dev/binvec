[package]
name = "binvec"
version = "0.1.0"
edition = "2024"
authors = ["baltdev"]
description = "Implements a binary vector modeled after std's Vec."
repository = "https://github.com/balt-dev/binvec"
